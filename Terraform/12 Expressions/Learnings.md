Terraform offers various operations like 
1. String Interpolation like `"PavanTest_${var.variablename}"`

2. Conditional Expressions like `local.env == "prod" ? "Production" : "Dev"`

3. Arithmetic and Logical Operators